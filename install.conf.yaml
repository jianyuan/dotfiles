- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.Brewfile: Brewfile

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [brew bundle --global, Installing homebrew packages]

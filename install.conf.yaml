- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.gitconfig:
    ~/.gitignore_global:

- clean: ['~']

- create:
    - ~/code

- shell:
  - command: brew bundle install
    stdout: true
